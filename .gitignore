# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Environment / secrets -- NEVER commit API keys
.env
.env.local
*.key

# OS/editor cruft
.DS_Store
.vscode/
.idea/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Local throwaway test scripts, never part of the repo
_scratch_to_delete/

# Ad hoc benchmark/diagnostic run outputs (regenerated by benchmark_tonst.py
# and the diagnostic scripts; the numbers that matter are extracted into
# research/*.md, these raw dumps aren't meant to be version-controlled)
*.json

# Stray local snapshot folder, not part of the project
Claude outputs/
